# ============ Web 框架 ============
fastapi==0.115.0
# 保留 [standard]：本模块依赖 WebSocket（附 websockets），版本精确锁定满足 §3.1
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ============ 数据校验与配置 ============
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ============ 数据库（异步，§3.4） ============
sqlalchemy==2.0.35
# SQLAlchemy 异步引擎的实际运行依赖：greenlet 负责在同步 ORM 与 asyncio 之间切栈，
# 缺失时 `import sqlalchemy.ext.asyncio` 直接失败。也是 .coveragerc 中
# `concurrency = greenlet` 的前提，勿删。
greenlet==3.5.6
asyncmy==0.2.10

# ============ 缓存 ============
redis==5.0.8

# ============ 认证授权 ============
PyJWT==2.9.0
passlib==1.7.4
bcrypt==4.0.1

# ============ AI 编排（LangChain 1.x） ============
langchain==1.3.11
langchain-openai==1.5.0
langchain-classic==1.0.0

# ============ HTTP 客户端（外部 API 调用） ============
httpx==0.27.2
aiofiles==24.1.0

# ============ 测试（仅开发环境） ============
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
# 测试库驱动：用例跑在临时 SQLite 上，与云库隔离（§10.2 / §13.1）
aiosqlite==0.22.1
